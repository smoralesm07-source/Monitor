# Dependencias del módulo de reconocimiento de entidades
# Typer 0.26+ incorporó internamente Click y puede dejar sin el módulo
# externo `click` que la CLI de spaCy todavía importa.
spacy>=3.8,<3.9
typer>=0.12,<0.26
click>=8.1,<9
